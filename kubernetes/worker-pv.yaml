apiVersion: v1
kind: PersistentVolume
metadata:
  name: worker-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/idata"
